import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const Search = () => {
  return (
    <View>
      <Text>Search</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
